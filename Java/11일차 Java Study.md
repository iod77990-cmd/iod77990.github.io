>- java 시간 class

### Time
- LocalDate
- LocalDatetime
- Zoneid
- 시스템 의 날짜와 시간을 가져오는 클래스, localDateTime.now() 객체를 만들어 현재시간을 가져와 출력할 수 있음
```java
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time Zoneld; // 글로벌 시간 타임존 적용하여 가져오는 역할

LocalDateTime now = LocalDateTime.now(); // 현제시간이 적용된 now 객체
```


